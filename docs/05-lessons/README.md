# 05-lessons — what broke, why, how to avoid
- 0001 · non-breaking space silently lost when writing source files
- 0002 · lockfile written on macOS breaks npm ci in the alpine image
- 0003 · Prisma 7 client fails to load under Jest CJS VM with module nodenext
- 0004 · ESM-only dependency (svix) fails to load under Jest on Node 22
- 0005 · svix Webhook.verify returns no body; the fake hid the adapter bug
